{
  "mcpServers": {
    "mcpeg-gateway": {
      "command": "./build/mcpeg",
      "args": ["gateway", "--dev"],
      "env": {
        "LOG_LEVEL": "debug",
        "MCP_DEBUG": "true"
      }
    }
  }
}